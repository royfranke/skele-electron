const PRELOAD_JSON = [
    {
        NAME: 'SAVES',
        PATH: {
            JSON: 'data/saves.json'
        }
    },
    {
        NAME: 'INPUTCONFIG',
        PATH: {
            JSON: 'data/config_input.json'
        }
    }
];

export default PRELOAD_JSON;
